@props([
    'message' => 'This Finance area is a foundation placeholder. Configuration and data will arrive in a later phase.',
])

<div class="card border-0 shadow-sm">
    <div class="card-body text-muted">
        {{ $message }}
    </div>
</div>
